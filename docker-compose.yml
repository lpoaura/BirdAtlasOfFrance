version: "3"

services:

  backend:
    image: lpoaura/odf:latest
    env_file: 
      - backend/.env
    container_name: odf-backend
    ports:
      - 8000:8000
    environment:
      POSTGRES_HOST: $POSTGRES_HOST
      POSTGRES_DB: $POSTGRES_DB
      POSTGRES_USER: $POSTGRES_USER
      POSTGRES_PASSWORD: $POSTGRES_PASSWORD
      POSTGRES_PORT: $POSTGRES_PORT
