version: "3"

services:

  backend:
    build: backend/
    container_name: odf-backend
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/:/app
    ports:
      - ${APP_PORT:-8000}:80
    environment: 
      DEBUG: "true"
      LOG_LEVEL: DEBUG

